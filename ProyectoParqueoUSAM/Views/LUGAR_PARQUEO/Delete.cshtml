@model ProyectoParqueoUSAM.Models.LUGAR_PARQUEO

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>LUGAR_PARQUEO</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NOMBRE_PARQUEO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NOMBRE_PARQUEO)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DESCRIPCION_D_PARQUEO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DESCRIPCION_D_PARQUEO)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
